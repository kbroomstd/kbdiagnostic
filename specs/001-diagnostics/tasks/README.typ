= Tasks

#link("001-core-model.typ")[`001-core-model`]
#link("002-renderer-interface.typ")[`002-renderer-interface`]
#link("003-source-context.typ")[`003-source-context`]
#link("004-formatting.typ")[`004-formatting`]

== Shared Done
- task file names match numbered work order
- each task defines exact exported symbols or fixtures
- each task lists at least one deterministic test command or golden check
- each task `Done` names concrete artifact, not vibe
